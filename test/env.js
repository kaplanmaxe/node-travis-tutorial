/**
 * This is a place where you can put your sensitive data to run tests locally.
 * It can contain things like database credentials, API keys, and more.
 * It should ALWAYS be placed in your .gitignore file.
 * Since this is an example, I am tracking it in the repo and using dummy data.
 */
module.exports = {
  name: 'Max Kaplan',
  encryptedName: 'Travis CI'
};
